/** As contas que `preparar.ts` cria, com ids fixos para as sessoes assinadas. */
export const ADMIN_ID = 'ad'.repeat(12);
export const CLIENTE_ID = 'c1'.repeat(12);
export const BASE = 'http://127.0.0.1:3200';
